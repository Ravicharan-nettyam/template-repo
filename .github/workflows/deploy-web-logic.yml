# deploy-dev-weblogic.yaml
name: Deploy to Dev WebLogic
on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Deploy
        run: echo "Deploying to WebLogic"